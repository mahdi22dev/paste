{
  "name": "pastenest",
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@8.4.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main",
    "ui": "pnpm -F apps/client dlx shadcn-ui@latest add"
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
